// Placeholder smoke test. Replace with real widget tests as the suite grows.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, equals(2));
  });
}
