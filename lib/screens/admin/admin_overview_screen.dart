import 'admin_command_center_screen.dart';

/// Admin "Overview" tab — currently a thin re-export of the Command Center
/// so the bottom-nav can land on the same dashboard without a second route.
typedef AdminOverviewScreen = AdminCommandCenterScreen;
